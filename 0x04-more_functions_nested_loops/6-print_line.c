#include "main.h"
/**
 * print_line - prints line
 * @n: the number of lines to draw
 * Return: empty
 */

void print_line(int n)
{
	int x;

	if (n > 0)
	{
		for (i = 0; i < n; i++)
		{
			_putchar('_');
		}
		_putchar('\n');
	}
	else
	{
		_putchar('\n')
	}
}
