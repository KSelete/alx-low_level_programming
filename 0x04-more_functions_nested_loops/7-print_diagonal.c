#include "main.h"

/**
 * print_diagonal - print diagonals
 * @n: the number of times to print diagonals
 * Return: void
 */

void print_diagonal(int n)
{
	int x, y;

	if (n <= 0)
	{
		_putchar('\n')
	}
	else
	{
		{
			for (y = 0; y < x; y++)
			{
				_putchar(32);
			}
			_putchar(92);
			_putchar('\n')
		}
	}
}
